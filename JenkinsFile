pipeline {
    /* insert Declarative Pipeline here */

agent any

stages {
        stage('Deploay/ Build App') {
            steps {
                sh 'pwd'
                sh 'ls -lart'
            }
        }
        
         stage('Frondend tests') {
            steps {
                sh 'pwd'
                sh 'ls -lart'
            }
        } 
        stage('Backend tests') {
            steps {
                sh 'pwd'
                sh 'ls -lart'
            }
        }
        stage('Performace tests') {
            steps {
                sh 'pwd'
                sh 'ls -lart'
            }
        }
        
        
    }
}